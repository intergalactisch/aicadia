# Entity Property evidence

> **Role / side:** Property delivery and evidence history / evidence bridge.
> **Authority:** owns deterministic Property proof, paid-candidate history and current validation status.
> **Excludes:** current Property semantics and runner operations; see `docs/game/` and `runner/agent-playtest.md`.

## Delivered deterministic evidence

The later uniform creation and combined Action evolution is complete; its
cross-state proof is owned by
[Uniform Entity-state evidence](entity-state.md).

The Agent-contract tests
`agent_contract_describes_the_exact_fifteen_player_capabilities`,
`agent_contract_teaches_property_trait_flow_without_background_authority`,
`control_like_property_and_trait_content_never_becomes_provenance`,
`trait_tool_descriptions_pin_creation_orientation_preview_and_response_boundaries`
and `trait_identifiers_remain_private_protocol_selectors` jointly pin the global
instructions and all fifteen tool descriptions. They retain every Entity's initial
Property state, canonical key/type reuse, complete multi-change preview,
Action-versus-Interaction meaning, structured-current precedence only for fictional
current meaning, content-versus-instruction separation, control-like content as
in-World content rather than infrastructure provenance, neutral presentation and no
background authority.

The historical Property-era fake controller remains contract evidence only for the
catalog published at its execution time. Its action role performed five MCP-only
grounding reads with one shared Place revision, and one explicitly confirmed
`introduce_entity` preview retains its complete initial `material = weathered cedar`
Property unchanged through the single commit attempt. It does not prove stochastic
Agent obedience, the full Property World outcome or the current replacement catalog.

That outcome is token-free deterministic World evidence. The named test
`every_entity_creation_route_atomically_establishes_one_hundred_properties` covers
all four initial-Property routes;
`property_action_changes_actor_place_ordinary_and_other_character_uniformly` covers
one explosion-shaped Action over actor, Place, ordinary Entity and another
Character; `interaction_property_changes_actor_and_target_without_authoring_a_response`
covers actor/explicit-target change without a target response; and
`current_property_read_paginates_local_facts_and_excludes_unplaced_entities` covers
outward local current values. Their accepted Activities carry the exact typed
history. `property_http_and_mcp_share_creation_bulk_change_history_and_strict_errors`
adds all four creation routes, the 100-write explosion, current local pagination,
actor/target Interaction, exact Activity hydration, retry identity, strict errors
and rollback through HTTP and MCP. Together these tests execute the scenario that
the Agent contract describes without claiming a model chose or narrated it
correctly.

## Paid candidate and current status

The accepted
[current Property and Trait validation plan](../../.agents/plans/20260814-204720-current-property-trait-live-validation/plan.md)
authorized one Property candidate with at most four `gpt-5.6-sol` high process
calls and zero retries. `run-TJ1sAwZ3` consumed that authorization and exactly three
calls. The grounded proposal, zero-tool exact initial-Property preview and single
confirmed `submit_action` commit all passed. The accepted World state contained the
marker's exact name and description, current `material = weathered cedar` Property
and matching `submit_action` Property history.

The candidate is nevertheless correctly classified `failed`: the independent HTTP
gate returned both the marker and the other co-located Character, while the
controller incorrectly required the complete Place Entity summary to contain only
one item. It stopped before the observer process; its pre-created observer evidence
files remain empty. Raw usage events total `274816` input, `172800` cached input,
`4086` output and `2094` reasoning-output tokens. Ownership-verified cleanup dropped
the disposable database and left no recovery action. The retained manifest has one
known telemetry mismatch: `cleanup.status` is `dropped` while
`deployment.status` remained stale at `ready`; the exact ownership-verifying drop
log proves the terminal database state. Historical evidence is not edited.

After that terminal result, the controller was corrected to require exactly one
matching marker without rejecting unrelated co-located Entities. Its fake HTTP and
observer fixtures now include the other Character, and usage is persisted after
every completed model process. The permanent fake suite passes, and a subsequent
public preflight again passed the current catalog, schemas, CLI/model boundary and
ownership-verified database create/drop with no `codex exec`. This token-free
controller also records `deployment.status: dropped` and clears the server address
after a successful owned drop. The
correction does not relabel the failed candidate, does not prove independent Agent
observation and did not authorize a retry under that completed plan.

## Post-correction validation result

The accepted
[post-correction validation plan](../../.agents/plans/20260814-223843-post-correction-property-trait-validation/plan.md)
authorized exactly one fresh candidate after both fake suites, focused World/server
parity and public zero-model preflight passed. `run-Cx1eI1zC` consumed that
authorization and all four `gpt-5.6-sol` high process calls with zero retries.
Proposal, withheld-selection preview, explicit-confirmation commit and authoritative
HTTP all passed. The World stored the exact marker, description,
`material = weathered cedar` current Property and matching Activity.

The independent observer also made the exact four permitted reads in order and
returned the authoritative marker id, Place id, Entity name/description, canonical
prose and Property. The candidate remains truthfully `failed`, however: its
`get_entity_at_current_place` call supplied the schema-valid optional `limit:100`,
while the then-current observer validator admitted only omission of that limit.
Consequently `phases.observer.status` is `failed` even though its retained MCP events
and strict final contain the intended proof. Success under the frozen runner ladder
is therefore not claimed and the historical manifest is not edited.

Recorded usage totals are `346266` input, `210944` cached input, `3436` output and
`1039` reasoning-output tokens. Both `deployment.status` and `cleanup.status` are
`dropped`; the exact ownership-verifying drop log records
`ownership_verified_and_dropped`, no process or recovery remains and no retry is
authorized.

The current controller now accepts an omitted limit or any exact schema-valid
integer from 1 through 100 on its one-page pageable reads while continuing to reject
cursors and all other argument keys. The permanent fake observer explicitly uses
`limit:100` for `get_entity_at_current_place`; the full token-free suite passes.
This fixes the live false negative for a future separately authorized candidate but
does not turn `run-Cx1eI1zC` into completed evidence. There is no unconsumed Property
authorization.
