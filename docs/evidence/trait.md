---
status: Deterministic delivery and complete paid establish/develop/independent-observer validation succeed; exact seven-call candidate, lineage, both HTTP gates and ownership-safe cleanup passed
---

# Entity Trait evidence

> **Role / side:** Trait delivery, validation and evidence history / evidence bridge.
> **Authority:** owns the complete deterministic and paid-candidate Trait saga, audits and current status.
> **Excludes:** current Trait semantics and runner operations; see `docs/game/` and `runner/trait-playtest.md`.

The completed deterministic delivery is recorded by the
[Trait delivery plan](../../.agents/plans/20260813-200829-entity-trait-development/plan.md).
The closed paid-validation boundary is recorded by the
[Trait live-validation plan](../../.agents/plans/20260814-111749-trait-live-validation/plan.md).
The later combined Property/Trait attempt is recorded by the
[combined validation plan](../../.agents/plans/20260814-204720-current-property-trait-live-validation/plan.md).
The successful post-correction attempt is recorded by the
[post-correction validation plan](../../.agents/plans/20260814-223843-post-correction-property-trait-validation/plan.md).

## Deterministic proof

The later uniform creation and combined Action evolution is complete; its
cross-state proof is owned by
[Uniform Entity-state evidence](entity-state.md).

T3–T6 prove the executable three-table lineage/current model, mixed Action and
Interaction establish/develop packages, exact thirteen-tool replacement, combined
current Entity state pages, full natural preview/confirmation contract, concurrency,
retry, rollback, bounded query behavior and adapter parity. Five permanent
Agent-contract tests pin the current global/tool guidance and exact generated
catalog. The dedicated [Trait runner](runner/trait-playtest.md) proves contextual
Action establishment, later Interaction development of the same stable lineage and
enriched Entity retrieval, including seventeen fail-closed injections, with
`model_calls: 0` and `paid_candidate: false`. T7 integration and independent review
completed with no P0–P3 finding. The last all-target run before the final
vacated-statement regression was 107/107; the subsequent focused regression and full
World suite pass 75/75, so no newer all-target total is claimed. None of the
historical runner or candidate evidence is relabelled as Trait evidence.

## Candidate and audit history

T8's reviewed live boundary was two Agents, two sessions, seven paid process calls
and zero retries, with authoritative HTTP gates and ownership-safe cleanup.
Independent T8 review covered the superseded path-bound digest
`3eb10e6ec1d375048dc96fb415ecad8c77b81f177c65138c315711d248d0f449`.
The a564 replacement candidate digest was
`a564ddedd49094dd70b02aad3eefdb39e6334b482d94859d6f61dea720fb1302`.
Candidate `candidate-MmwRmcBv` used the immediately preceding digest
`f38ed39b7a100ee09cca46743b3b9398f46ccb3d4999f4591f478113fb2b4fa3`,
consumed its authorization and invoked Codex for exactly one model process call. It
failed before any Agent MCP read or post-setup Agent submit/Trait mutation because
MCP revision `2026-07-28` was not enabled. Ownership-verified cleanup dropped its
database and retained the evidence.

After T9R GO, the User explicitly authorized one paid a564 replacement using only
MCP `2026-07-28`. `candidate-ydttdFfc` consumed that authorization and one model
process call (`68117` input, `50176` cached input, `798` output and `419` reasoning
tokens). Exactly `get_world`, `get_character`, `list_entity_at_current_place` and
`list_activity_at_current_place` completed, and the strict final contained three
valid grounded proposals. The controller then falsely rejected valid fractional
RFC3339 timestamps from the latter affected outputs. No preview, post-setup Agent
submit, Trait mutation or candidate HTTP gate ran. Ownership-verified cleanup
dropped the database; independent audit returned GO for this exact failed claim with
one P1 at `validate_tool_output` and no other finding.

Archive `.aicadia-trait-playtest/archive-original-f38-MmwRmcBv` preserves all 68
original files with private permissions and unchanged content/permission
fingerprints. Token-free `preflight-cEeVelIZ` returns GO for a564 with
`candidate_started=false`, `authorization_consumed=false`, `codex_invoked=false`,
`model_calls=0`, thirteen equal runtime tools, six passed schemas and an
ownership-verified dropped database. Independent audit found only the stale-status
P3 corrected here; focused independent re-review returned GO with no P0–P3 finding
and completed T9R. T10 accepts the real UTC fractional-second Chrono form while
requiring exact normalized parse/format roundtrip. The post-failure runner at that stage
digest is `6649959c7f230f2240f8d1b7e67081c20c473c00654ef36409fa439a8d85a824`.
Initial final review's sole P1/P2 were corrected; second independent Terry/ownership
review returned GO with no P0–P3 finding. No retry or Trait live gameplay outcome
was earned by that correction.

## Closed live-validation result

Private `archive-replacement-a564-ydttdFfc` contains all 64 unchanged a564
historical files plus its private algorithm index, and the original 68-file f38
archive remains unchanged. Public token-free `preflight-HMxwGPCF` returned GO for
digest `6649959c7f230f2240f8d1b7e67081c20c473c00654ef36409fa439a8d85a824`:
`candidate_started=false`, `authorization_consumed=false`, `codex_invoked=false`,
`model_calls=0`, current-only MCP `2026-07-28`, 13 live-equal tools, 6 schemas and
cleanup `ownership_verified_and_dropped`. The initial T3 review found exactly two P3
findings—the index lacked an explicit serialization recipe and current authorities
were still prospective—and both are corrected. Independent re-review returned GO
with no P0–P3 finding, completing T3 readiness.

Exact-digest `candidate-63hjH4HW` then consumed its authorization and two model
calls. The Action proposal passed after the four current-MCP reads `get_world`,
`get_character`, `list_entity_at_current_place` and
`list_activity_at_current_place`. Its zero-tool Action preview exited 0 with strict
valid JSON and exact `entity_name: "Pip"`, then failed solely because live
`validate_action_preview` required `startswith("Pip ")` while the prompt and
permanent fake contract require exact `Pip`. No Action commit, candidate HTTP gate,
Interaction or Mara phase ran; submits, post-setup Trait mutation and live success
are zero. Total usage was 148068 input, 100352 cached input, 1722 output and 867
reasoning-output tokens. Cleanup was `ownership_verified_and_dropped`, evidence is
private, no process remains and no retry or new authorization exists.

Final independent T5 review returned GO for the exact completed-as-failed alignment
with P0/P2/P3=0. The known deferred P1 is live Action `startswith("Pip ")` and
analogous unreached Interaction `startswith("Pip ")`/`startswith("Mara ")` drift
against prompt/fake exact names. No fix, retry, live success or new authorization
was added. Trait live validation is closed and no active edge or paid authorization
remains.

## Combined-validation failure and post-correction archive

After the exact-name live validators were corrected and the fresh digest
`5bdfa2e0fd49f0a0cce7e27453e5b2e803e4206d992a113f50156d50f8a4061f`
passed its token-free suite and public preflight, `candidate-rI28yzhw` consumed the
new plan's one Trait authorization. It used exactly three `gpt-5.6-sol` high process
calls. The grounded Action proposal, zero-tool preview with exact natural
`entity_name: "Pip"`, and one confirmed `submit_action` all passed. The accepted
World result established stable Trait
`b30f9637-3e9c-4cf9-bcf6-e86e0de50234` on Pip with the exact frozen statement and
matching Activity.

The candidate is correctly `failed` rather than live Trait success. Its first
authoritative HTTP files contain exactly that current Trait and establishment
history, but `validate_establish_http_gate` evaluated an unparenthesized
`and [selection] | length == 1` expression as a boolean piped into `length`. The
controller stopped there. No Interaction proposal, development, development gate or
Mara process started. Recorded usage totals `262392` input, `168704` cached input,
`3329` output and `1253` reasoning-output tokens. Cleanup is
`ownership_verified_and_dropped`; no recovery or process remains.

The retained manifest has one known reached-phase telemetry mismatch:
`phases.http_establish_gate` remains `pending` even though the two HTTP files and
terminal validator error prove that the gate ran and rejected them. The historical
manifest is not edited; every later phase was genuinely unstarted.

The post-failure controller moves establishment and development HTTP assertions into
shared file validators, parenthesizes both affected array counts and executes both
validators in the permanent token-free fake suite. Per-process usage is also stored
before terminal validation, and a reached rejected HTTP gate is recorded `failed`
instead of remaining indistinguishable from an unstarted gate. That suite passes.
The exact post-correction runner digest at that stage is
`e940c03e4836f92b1d6b9d1219c1709b32cdd815a0d99d3721ae274c7513ae84`.
The consumed sentinel and `candidate-rI28yzhw` are preserved unchanged beneath the
private mode-700 `archive-post-correction-rI28yzhw` archive. Its index proves 55
preserved paths with relative-path, content and permission SHA-256 fingerprints.
This archive operation did not relabel the failed candidate or complete the Trait
development/observation scenario. At archive closure, the accepted post-correction
validation plan authorized exactly one new candidate conditional on token-free
readiness; no new candidate or model process existed yet.

The command that produced this terminal candidate was:

```sh
DATABASE_URL='postgres://localhost/postgres' tools/trait-playtest run \
  --confirm-token-spend \
  --candidate-digest 5bdfa2e0fd49f0a0cce7e27453e5b2e803e4206d992a113f50156d50f8a4061f
```

## Post-correction live success

Both permanent fake suites, 16 focused Property World tests, 12 focused Trait World
tests, both focused server-parity tests and both public preflights passed before
spend. Trait preflight invoked no model, started no candidate, consumed no
authorization and ownership-verified its database drop. The live digest remained
exactly `e940c03e4836f92b1d6b9d1219c1709b32cdd815a0d99d3721ae274c7513ae84`
before and after preflight.

Exact-digest `candidate-idl47NKn` then consumed the one authorization and completed
exactly seven `gpt-5.6-sol` high process calls with zero retries. Pip's grounded
Action proposal, zero-tool natural preview and one confirmed commit established
stable Trait `0495bf42-da65-4989-93d4-1f15f7814222` with statement
“Waits for the second echo before springing —
aicadia-trait-live-20260814T205324Z-14372-545.” The first authoritative HTTP gate
proved that statement, stable identity and matching Action Activity.

The same Pip session then grounded a later outward Interaction targeting Mara,
previewed one development and committed it once. It retained that exact stable Trait
id, predecessor and target while changing the current statement to “Reads Mara’s
second footfall through the returning echo —
aicadia-trait-live-20260814T205324Z-14372-545.” The second HTTP gate proved lineage,
directed Activity and no Trait on Mara. Mara's separate session independently
derived Pip's id from compact orientation, fetched his current Trait and reported
both current and predecessor statements plus the matching Interaction prose.

All nine frozen phases and terminal validation are `passed`; `run_status` is
`completed`. Recorded totals are `1034774` input, `649984` cached input, `12781`
output and `5454` reasoning-output tokens. The sentinel records the exact accepted
digest. Deployment is `dropped`, cleanup is `ownership_verified_and_dropped`, no
recovery or process remains and the authorization is consumed. This is the first
complete paid Trait establish/develop/independent-observer success; it adds no new
game capability or second-candidate authority.

## Retained paid command

The frozen command that produced failed `candidate-ydttdFfc` was:

```sh
DATABASE_URL='postgres://localhost/postgres' tools/trait-playtest run \
  --confirm-token-spend \
  --candidate-digest a564ddedd49094dd70b02aad3eefdb39e6334b482d94859d6f61dea720fb1302
```

At that historical closure, that authorization was consumed and its then-active
sentinel forbade another candidate. No retry, other candidate or protocol fallback
was authorized by that plan.

## Current token-free runner state

The uniform Entity-state build aligned the current catalog and fake Action shape to
`change_entity_state` without invoking a model or changing retained candidates. Both
permanent fake suites pass, and the exact current Trait runner digest is
`1720e1052cf2a2d4823395f7c667132a61f97b5aab5031fa347be6c55d344f34`.
