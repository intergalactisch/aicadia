# Local play and Studio evidence

> **Role / side:** local launcher, Agent handoff, ledger and Studio delivery history / evidence bridge.
> **Authority:** owns retained deterministic completion records for persistent local play and its read-only browser surfaces.
> **Excludes:** current local-operation and Studio contracts; see `docs/game/local-play.md`.

## Completed boundary

The current delivered slice is one non-destructive launcher, one private ignored
profile, one same-origin Rust Aicadia Studio, isolated Character-onboarding Agent
operation and persistence/browser evidence. Studio absorbed the earlier ledger.
`Game` projects allowlisted repository authorities and the exact compiled MCP
catalog; `Live` projects bounded local World data and bounded current public-schema
structure. This proves local development visibility and persistence, not model
output quality,
complete documentation coverage, public hosting, retained schema history/diffing or
multiple-World delivery.

## Unified Aicadia Studio evidence — 2026-08-17

- one fixed Atlas-derived application shell replaced the three comparison modes;
  its Game/Live, source-heading, nine model/seam, fifteen exact MCP-tool,
  development-status, Entity, Activity and storage-table selections survive reload
  through stable query links;
- copied references combine the deep link with owning source, storage or durable
  record context, while browser assets still author no rule, model field, MCP schema
  or database definition;
- a live `aicadia_local` PostgreSQL capture returned fourteen public application
  tables, 61 columns, 41 non-foreign-key constraints, 32 ordered foreign keys and 37
  indexes with latest migration 10; two reads had different capture times and the
  same structural SHA-256 fingerprint;
- the explicit snapshot route returned pretty JSON with attachment filename
  `aicadia-schema-snapshot.json`; unit and integration checks prove structural
  fingerprint stability, ordered composite foreign keys, exact download headers and
  fail-closed table/column/constraint/relation/index bounds;
- in-app browser inspection covered the unified Game and Live over real local data
  at desktop and 390 px, direct source/model/Entity/Activity/storage routes,
  relative source links, copy-to-clipboard, responsive mobile navigation, bounded
  loaded-record filtering, zero horizontal overflow and an empty warning/error
  console;
- Activity detail linked two explicitly involved Entities in the exercised local
  record, model pages joined owning Markdown meaning to realized PostgreSQL columns
  and relations, and Storage exposed definitions without reading World rows; and
- strict Clippy, Rust formatting, JavaScript and Bash syntax, diff integrity, all
  161 executed Rust tests and the complete disposable launcher/isolated-Agent
  lifecycle passed; one accepted-contract fixture generator remained explicitly
  ignored and no model was invoked.

This evidence proves the current loopback development projection and the exercised
live PostgreSQL/browser seams. It does not prove arbitrary future repository/schema
sizes, browser gameplay, a public security boundary, retained snapshot comparison or
multiple-World behavior.

## Studio prototype evidence — 2026-08-17

- `cargo dev` still starts one Rust server process and now prints/opens one
  `Studio URL`; database, stable User, MCP and explicit Agent handoff lifecycles
  remain unchanged;
- focused Rust tests render fourteen allowlisted repository sources, extract six
  current model sections, reuse the exact fifteen-tool compiled MCP catalog, reject
  invalid page limits and expose fourteen allowlisted PostgreSQL tables with their
  exact columns and foreign-key relations;
- the root and Studio assets contain only GET fetches and no form, browser mutation,
  MCP endpoint or authored model/tool/rule catalog;
- Atlas, Workbench and Observatory render the same real catalog and local-World
  snapshot through shareable `variant` and `section` parameters;
- desktop and 390px browser inspection covered all three layouts, Game/Live
  switching, live Entity selection with current Property/Trait state, responsive
  navigation and an empty warning/error console; and
- Studio adds no OpenAPI path, MCP tool, game schema, game mutation, model call,
  background poll or durable World identity.

The final validation ladder passed with 158 Rust tests, one explicitly ignored
catalog-generator test, strict Clippy, formatting, Bash syntax, the complete
disposable launcher lifecycle and diff integrity. The retained earlier ledger
evidence follows.

## Earlier ledger completion evidence

- the lifecycle suite provisions one User and reuses that same UUID after restart;
  missing-profile, concurrent-launch, corrupt/stale-profile and occupied-port cases
  all fail closed without a second User or listener;
- disposable integration state retained the identical User, Character, Place, three
  Entity records, four Activity records and canonical prose across restart, both in
  canonical HTTP JSON and a fresh browser page;
- browser network and source boundaries are GET-only and contain no gameplay input;
- MCP initialization and the exact catalog publish the Character workshop while
  retaining the existing action workshop;
- desktop, mobile, pagination, keyboard disclosure and pre-Character browser checks
  pass without exposed User UUID, viewport overflow or browser errors;
- Bash syntax and lifecycle, formatting, strict Clippy, all 59 Rust tests,
  `git diff --check` and independent risky-seam review pass; and
- current contract, concept log, backlog item and accepted plan agree. The explicitly
  disposable evidence database, state directory, listener and browser tabs were
  removed after ownership checks; unrelated working-tree changes were preserved.

## Agent host handoff evidence

The 2026-08-12 completion passed current discovery and catalog tests for the then
thirteen unchanged capability schemas and annotations, formatter, all-target and
all-feature Clippy with warnings denied, all 58 Rust tests, the disposable local
launcher/adapter lifecycle and diff integrity. The lifecycle preserved one stable
User, started fake Codex in an empty external workspace with isolated home and
configuration, injected the exact player contract, required current Aicadia MCP,
removed its owned root and reported `codex_invoked=false`. Independent review found
no P0-P3 issue. No test database, temporary player root, new listener or process
remained; the pre-existing server on port 3000 was deliberately untouched.

## Cargo developer entrypoint

The 2026-08-14 developer-entrypoint completion made `cargo dev` the supported thin
entry into the unchanged local launcher. A real isolated first start from the normal
shell environment discovered the installed keg-only Homebrew PostgreSQL client,
created and migrated its disposable database, provisioned exactly one User and
served that User through the printed ledger context. Ctrl-C followed by a second
`cargo dev` returned the identical User UUID. The full disposable launcher/adapter
lifecycle, the Cargo bridge's strict Clippy check, Rust formatting, Bash syntax and
diff integrity passed. The explicitly named verification database and private state
directory were removed after exact ownership checks; the pre-existing database,
profile and server on port 3000 were untouched.
