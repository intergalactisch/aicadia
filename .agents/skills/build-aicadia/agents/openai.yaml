interface:
  display_name: "Build Aicadia"
  short_description: "Handle or plan the next Aicadia build"
  default_prompt: "Use $build-aicadia to execute a qualifying micro-change directly or create an aligned plan and wait for acceptance before consequential Aicadia work."
policy:
  allow_implicit_invocation: true
