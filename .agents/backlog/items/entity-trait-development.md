# Local Entity Trait development

Status: Done

## Outcome

Contextual confirmed Actions and Interactions can establish and develop concise
Entity-owned Traits with stable identity and immutable Activity-backed earlier
statements. Whenever an Agent actually fetches its Character or one exact-local
Entity, one bounded combined page carries current Property/Trait associations; the
Agent decides their relevance and World stores no observer-specific knowledge. The
accepted deterministic slice now executes through PostgreSQL, World, HTTP, MCP and
the permanent Agent contract. T7/T8 review is complete, the original paid candidate
is completed-as-failed, and token-free readiness/audit for exactly one accepted MCP
`2026-07-28` replacement is complete with independent GO.

The completed delivery plan is
[`../../plans/20260813-200829-entity-trait-development/plan.md`](../../plans/20260813-200829-entity-trait-development/plan.md).
The active accepted validation plan is
[`../../plans/20260814-111749-trait-live-validation/plan.md`](../../plans/20260814-111749-trait-live-validation/plan.md).
The design record is
[`docs/concept/11-entity-traits-and-change.md`](../../../docs/concept/11-entity-traits-and-change.md).
[`docs/game/`](../../../docs/game/README.md) publishes the current executable
contract. T3–T8 have delivered deterministic behavior, exact thirteen-tool parity,
Agent guidance, fake evidence, the original readiness audit and replacement T9R.
The one authorized paid a564 replacement is completed-as-failed before preview, with
no post-setup Agent submit or Trait mutation. T10 is complete after second
independent Terry/ownership review returned GO with no P0–P3 finding; no live Trait
success, retry, new authorization or other candidate was earned.

## Completed live-validation result

The completed validation plan leaves no `Now / Active` edge. T2 completed: private
`archive-replacement-a564-ydttdFfc` preserves 64 historical files/3 directories/67
paths byte- and permission-identically plus one mode-600 algorithm index, while the
original 68-file f38 archive remains unchanged. Public token-free
`preflight-HMxwGPCF` then returned GO for digest
`6649959c7f230f2240f8d1b7e67081c20c473c00654ef36409fa439a8d85a824`:
`candidate_started=false`, `authorization_consumed=false`,
`codex_invoked=false`, `model_calls=0`, only MCP `2026-07-28`, 13 live-equal tools,
6 schemas and database cleanup `ownership_verified_and_dropped`.

The initial independent T3 review found exactly two P3 findings: the private index
needed an explicit serialization algorithm and current authorities still described
T2/T3 prospectively. Both are corrected; independent re-review returned GO with no
P0–P3 finding, completing T3 readiness.
All three paid candidates remain exact failures. `candidate-63hjH4HW` consumed the
fresh authorization for digest
`6649959c7f230f2240f8d1b7e67081c20c473c00654ef36409fa439a8d85a824`
and used two `gpt-5.6-sol` high model calls over only MCP `2026-07-28`. Its Action
proposal passed after exact reads `get_world`, `get_character`,
`list_entity_at_current_place` and `list_activity_at_current_place`. The tool-free
Action preview exited 0 with strict valid JSON and `entity_name: "Pip"`, then failed
solely because live `validate_action_preview` required `startswith("Pip ")` while
the prompt and permanent fake contract require exact `Pip`. No Action commit,
candidate HTTP gate, Interaction or Mara phase ran; there were zero submits, no
post-setup Trait mutation and no live success. Usage was 148068 input, 100352 cached
input, 1722 output and 867 reasoning-output tokens. Cleanup was
`ownership_verified_and_dropped`, no process remains, evidence is private and no
retry or new authorization exists.

Independent T5 review returned GO for the completed-as-failed alignment with
P0/P2/P3=0. The known deferred P1 is live Action `startswith("Pip ")` and analogous
unreached Interaction `startswith("Pip ")`/`startswith("Mara ")` drift against the
prompt/fake exact names. No fix, retry, live success or new authorization was added.
The User chose draft
[`../../plans/20260814-130554-documentation-architecture/plan.md`](../../plans/20260814-130554-documentation-architecture/plan.md),
to be picked up next; it is neither edited nor activated here. Trait live validation
is Done and no `Now / Active` edge is opened by this closure.

## Confirmed direction

- Trait is a concise explanatory Entity characterization, not a Property, status,
  score or executable rule.
- A Trait can develop; User intent/confirmation, Agent exact authorship and World
  validation/write remain separate.
- Traits are never initial creation input; they arise contextually through accepted
  play.
- Action may uniformly affect actor, Place, ordinary Entity and other Character.
- Establishment creates a stable id; development appends immutable versions and
  advances one current pointer; retirement is deferred.
- The unique root version is the sole establishing Activity provenance; development
  names stable Trait id/new statement while expected Place revision plus the locked
  current pointer select its predecessor atomically.
- Both Action and Interaction may establish/develop through one private writer;
  Interaction actor and explicit targets are uniformly eligible and it never authors
  a target response.
- Every Entity owns its Property/Trait state. `get_character` and every actual fetch
  of another eligible Entity return correct current associations; the Agent decides
  relevance. No observer-specific Knowledge/Observation or Relationship state exists.
- `list_entity_at_current_place` remains compact; `get_character` and new scoped
  `get_entity_at_current_place` use one combined typed association page with one
  cursor, default 25 and maximum 100. Activity/mutation references stay compact. The
  new read replaces the standalone Property list for exactly thirteen player tools.
- One Action `trait_change[1..100]` and optional Interaction
  `trait_change[0..100]` may mix establishment/development; Interaction Trait and
  Property consequences may coexist atomically.
- Exact normalized active duplicates and no-op development reject; semantic
  contradictions remain accepted and one lineage supersedes only itself, with no
  inferred Property/description precedence.
- Only explicit confirmed Action/Interaction are executable causes; no external
  writer is delivered.
- Trait prose is non-executable.
- Every immutable statement is trimmed non-NUL PostgreSQL `text` of 1–4,000 Unicode
  characters; richer causal story remains Activity prose.
- Agent authors and fully previews the exact natural Trait consequence; User accepts
  or rejects the whole package and receives no direct Trait editor.
- Every accepted mutation must retain immutable Activity provenance.
- Trait is the selected edge after delivered Property state and before queued
  investigation/discovery.
- The explicit 2026-08-14 spend GO authorized only the exact a564 replacement after
  T9R GO. That authorization is consumed; it authorizes no retry, other candidate or
  protocol fallback.

## Delivery boundary

Rounds 1–3 resolve every material behavior, interface, storage and evidence choice.
Schema, World behavior, HTTP/MCP parity, generated catalog, Agent contract, fake
controller, T7 review and T8 audit are delivered. Original candidate
`candidate-MmwRmcBv` at digest
`f38ed39b7a100ee09cca46743b3b9398f46ccb3d4999f4591f478113fb2b4fa3`
consumed its authorization and one model process call, then failed before any MCP
Agent MCP read or post-setup Agent submit/Trait mutation because Codex lacked explicit current MCP `2026-07-28`
activation. Ownership-verified cleanup dropped its database; its evidence and
sentinel are preserved in an archive inside the ignored evidence root. The User
accepted exactly one replacement boundary. Archive
`.aicadia-trait-playtest/archive-original-f38-MmwRmcBv` now preserves all 68 original
files with private permissions and unchanged content/permission fingerprints.
Token-free `preflight-cEeVelIZ` returns GO for corrected digest
`a564ddedd49094dd70b02aad3eefdb39e6334b482d94859d6f61dea720fb1302`
with no candidate start, authorization consumption or Codex invocation, zero model
calls, thirteen equal runtime tools, six passed schemas and ownership-verified
dropped database. Independent audit found only the stale-status P3 corrected here;
focused independent re-review returned GO with no P0–P3 finding, completing T9R.
Paid replacement `candidate-ydttdFfc` then consumed the one a564 authorization and
one model process call (`68117` input, `50176` cached input, `798` output and `419`
reasoning tokens). Exactly four current-MCP reads and a strict valid three-proposal
final completed before the evidence validator falsely rejected fractional RFC3339
timestamps from `get_character` and `list_activity_at_current_place`. No preview,
post-setup Agent submit, Trait mutation or candidate HTTP gate ran; ownership-verified cleanup dropped the
database. Independent audit returned GO for that exact failed claim with one P1 and
no other finding. T10 corrects that validator and binds the post-failure runner to digest
`6649959c7f230f2240f8d1b7e67081c20c473c00654ef36409fa439a8d85a824`.
The later T4 candidate is the exact two-call failure recorded in the current
live-validation status above.
Initial final review returned NO-GO only for missing exact calendar roundtrip P1 and
setup-obscuring evidence wording P2. Both were corrected; second independent
Terry/ownership review returned GO with no P0–P3 finding. The later candidate earned
no Trait live gameplay outcome or retry.

## Dependencies

- delivered Character-grounded Interaction and knowledge;
- delivered Local Entity Property state and thirteen-capability player contract;
- delivered Trait schema, World, adapters, catalog, Agent contract, fake runner and
  T7/T8 review; preserved original failed evidence is the dependency for replacement
  readiness and archive-safe proof.

## Non-goals

Initial creation Traits, retirement/reactivation, semantic inference, mechanics,
direct storage editing, automatic all-local visibility, remote/global Trait access,
relations, movement, unaccepted autonomous factors, background Agents and changes to
historical live-evidence byte content; the accepted archive changes only its path
after fingerprint and permission verification.

## Completion evidence

- all four creation routes remain Trait-free and reject Trait input;
- contextual Action establishment and Interaction development prove stable identity,
  uniform Action scope, actor/target Interaction eligibility, exact Activity history
  and correct enriched Entity fetches without observer state;
- duplicate/no-op, contradiction, stale/unavailable, retry, concurrency, deadlock,
  rollback, query-count and 100-row `EXPLAIN` evidence passes;
- combined first/continuation state pages preserve one Entity/Place revision at
  1/100-row bounds; compact orientation/mutation/history references never hydrate
  current state recursively;
- HTTP/MCP parity, enriched paginated `get_character`, removed flat Property tool,
  added scoped Entity fetch and exact runtime-generated thirteen-tool catalog pass;
- exact 1/4,000/4,001 statement bounds and full-preview/no-submit-before-confirmation
  Agent-contract cases pass;
- Agent-contract tests and a dedicated token-free fake Trait runner prove contextual
  Action → Interaction → enriched Entity fetch discipline;
- original evidence is preserved as 68 files in one mode-700 ignored archive with
  private permissions and identical content/permission fingerprints, leaving the
  active evidence root clean;
- token-free independent replacement GO proves exact digest, current-only MCP
  `2026-07-28`, preserved failed evidence, call/retry discipline and cleanup before
  any spend request;
- the one `gpt-5.6-sol` high replacement retains its exact pre-mutation failure,
  usage and ownership-safe cleanup without becoming a full candidate success;
- the post-failure fake flow accepts valid UTC fractional seconds in both affected
  outputs and rejects impossible or malformed date-time through exact normalized
  roundtrip before any model call;
- final review aligns current docs, concept/log, backlog/map and exact evidence with
  no P0–P3 finding or unrelated regression.
