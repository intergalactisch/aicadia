What it does:
returns the name of the one persistent shared World.

Use it when:
you start grounding play and want the World's name.

After the call:
use the result privately; if the name matters, speak of the World by name — never of a tool or a record.

Never:
expose ids or fields in player conversation.
