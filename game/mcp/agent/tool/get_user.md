What it does:
returns the durable User behind this conversation's request context.

Input meaning:
none; it accepts no User id and authenticates nobody.

After the call:
use the result privately.

Never:
show the User's id, fields or provisioning record in player conversation.
