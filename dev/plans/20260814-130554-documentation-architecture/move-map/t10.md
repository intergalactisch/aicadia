# T10 code decomposition move map

This fragment records the pure source and test moves performed by T10. T9 owns
repointing live cross-tree references to the former flat paths.

| Previous path | Current module home |
|---|---|
| `game/src/world.rs` | `game/src/world/mod.rs`, with domain models in `model.rs`, errors in `error.rs`, Property persistence in `property.rs`, Trait persistence in `entity_trait.rs`, Activity persistence in `activity.rs`, shared storage helpers in `common.rs`, reads in `read.rs` and mutations in `mutation.rs` |
| `game/src/wire.rs` | `game/src/wire/mod.rs`, with request types in `input.rs`, response types in `output.rs`, wire errors and cursor codecs in `error.rs`, and unit tests in `test.rs` |
| `tests/world.rs` | `tests/world/main.rs`, with suites in `action.rs`, `activity.rs`, `entity.rs`, `interaction.rs`, `property_behavior.rs`, `property_schema.rs`, `trait_behavior.rs` and `trait_storage.rs` |
| `tests/server.rs` | `tests/server/main.rs`, with suites in `action.rs`, `concurrency.rs`, `entity_trait.rs`, `error.rs`, `interaction.rs`, `property.rs` and `protocol.rs` |

## Required stitching

- Nineteen moved test fixtures use one additional `../` segment: seventeen
  migration includes in the World target, plus the Agent instruction and catalog
  fixture includes in the server target.
- Former file-private helpers needed by sibling modules use only `pub(super)`
  visibility. The existing public exports and `src/lib.rs` surface are unchanged.
- `dev/tests/trait-playtest.sh` injects digest drift into the existing post-split
  `game/src/world/mutation.rs` source. Its rejected public gates compare deterministic
  private candidate-state snapshots before and after; CLI-version and digest-binding
  checks accept the current computed digest only inside disposable candidate copies,
  leaving the closed live digest, sentinel and evidence untouched.
